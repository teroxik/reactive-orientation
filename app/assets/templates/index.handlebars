<div class="padding-top-bottom-left">
    <legend>Server IP Address</legend>
    <p>Use one of the following ip addresses to connect from a mobile device. It will be most likely the third one starting with 192 or 10 (LAN).
    The standard port is 9000. You should see the actual port number in the browser address bar.
    </p>
    <b><span id="ipInApp"></span></b>
</div>
<div class="padding-left">
    <table class="table">
        <tbody>
            {{#each }}
                <tr>
                    {{this}}
                    <td width="300px">
                        Aplha [rad]: {{ orientationdata.alpha }} <br/>
                        Beta [rad]: {{ orientationdata.beta }}   <br/>
                        Gamma [rad]:{{ orientationdata.gamma }}  <br/>
                    </td>
                    <td>
                        <div id="canvas{{unbound deviceId}}" class="canvas">
                            <img url="{{unbound canvas}}"/>
                        </div>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>